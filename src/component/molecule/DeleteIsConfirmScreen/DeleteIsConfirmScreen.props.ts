import type { DeleteIsConfirmScreenProps } from "./DeleteIsConfirmScreen.type";

const defaultProps: DeleteIsConfirmScreenProps = {};

export const propObj: { [key: string]: DeleteIsConfirmScreenProps } = {
  default: defaultProps,
};
